export type SubmitResult = {
  uid?: string;
};

// Placeholder: in a later phase, move encode + upload + sign + relay logic here
export async function submitAttestationFromWizard(): Promise<SubmitResult> {
  // TODO: implement with existing single-page flow logic
  return {} as SubmitResult;
}

